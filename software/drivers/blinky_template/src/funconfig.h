#ifndef _FUNCONFIG_H
#define _FUNCONFIG_H

// ch32v003fun compile-time configuration. The board definition already selects
// the CH32V003 target and its default 48 MHz HSI+PLL clock, so the defaults are
// fine for this blinky. See ch32fun.h for the full list of FUNCONF_* options.

#endif
